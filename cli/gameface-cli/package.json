{
  "name": "gameface-cli",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "bin": {
    "gameface-cli": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "npm run build && vitest run",
    "test:watch": "vitest",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build",
    "publish:package": "npm i&&npm run build&&npm publish"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "detect-indent": "^7.0.2",
    "tiged": "^2.12.8"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
